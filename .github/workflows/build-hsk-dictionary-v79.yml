name: Build HSK Dictionary v79

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - scripts/build_hsk_dictionary_v79.py
      - scripts/build_dictionary_stats_v79.js
      - scripts/apply_hsk_roast_v80.js
      - .github/workflows/build-hsk-dictionary-v79.yml
      - tests/v79-*.test.js
      - tests/v80-*.test.js
      - assets/v79/hsk-dictionary-v79.js
      - assets/v79/unified-dictionary-v79.js
      - assets/v79/unified-dictionary-v79.part*.txt
      - assets/v79/unified-dictionary-v79.css
      - assets/v79/home-copy-v79.js
      - assets/v80/hsk-roast-context-v80.1.js
      - community.js
      - pinyin-pro.loader.js

permissions:
  contents: write

concurrency:
  group: build-hsk-dictionary-v79
  cancel-in-progress: false

jobs:
  build:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Apply HSK Roast Mode cache key
        run: node scripts/apply_hsk_roast_v80.js

      - name: Generate HSK dictionary chunks
        run: python scripts/build_hsk_dictionary_v79.py

      - name: Calculate exact unified dictionary totals
        run: node scripts/build_dictionary_stats_v79.js

      - name: Validate generated data, dictionary and HSK Roast Mode
        run: node --test tests/v79-hsk-dictionary-data.test.js tests/v79-unified-dictionary.test.js tests/v80-hsk-roast-context.test.js

      - name: Commit generated dictionary and deploy key
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html assets/v79/hsk-dictionary assets/v79/dictionary-stats.json
          if git diff --cached --quiet; then
            echo "Generated data and deploy key are already up to date."
          else
            git commit -m "Generate HSK data and deploy Roast Mode v80.1"
            git push origin HEAD:main
          fi
