(function (root) {
  "use strict";
  if (root.__VDUCKIE_DEVELOPER_PREVIEW_V93_REMOVED__) return;
  root.__VDUCKIE_DEVELOPER_PREVIEW_V93_REMOVED__ = true;
  /* V108: the legacy sidebar renderer was removed. Authorization and mounting now live in developer-control-center.js. */
  root.VDuckieLegacyDeveloperPreview = Object.freeze({ version: "108.0", removed: true });
})(window);
